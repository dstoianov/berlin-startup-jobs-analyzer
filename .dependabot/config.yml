version: 1

update_configs:
  - package_manager: "python"
    directory: "/"
    update_schedule: "weekly"
    # Apply default reviewer and label to created
    # pull requests
    default_reviewers:
      - "dstoianov"
#    default_labels:
#      - "dependabot"
